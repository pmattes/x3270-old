
main()
{
	int i;

	for (i = 0; color_fallbacks[i]; i++)
		printf("color %d: %s\n", i, color_fallbacks[i]);
	for (i = 0; mono_fallbacks[i]; i++)
		printf("mono %d: %s\n", i, mono_fallbacks[i]);
	exit(0);
}
